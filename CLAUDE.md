# CLAUDE.md — Material Girl 한글패치 작업 가이드라인

높은 품질의 한글패치는 **번역 품질**과 **기술 품질** 두 축을 모두 잡아야 한다.
이 문서는 이 리포지토리에서 번역·검수·패치 작업을 할 때의 구속력 있는 규칙이다.

## 0. 프로젝트 구조 요약

- 게임: Steam판 Material Girl (RPG Maker MV / NW.js 0.12 세대 = Chromium 41 수준)
- 방식: 영어(En) 언어 슬롯을 한국어로 교체. 시나리오는 XOR-255 인코딩된 `.sl`
- 번역 소스: `source_en/`(영어 기계번역, 저품질) + `source_ch/`(중국어판, **의미 판단의 기준**)
- `source_data/`: 패치 대상 데이터 JSON의 **원본 사본**. `build_patch.py`는 여기서만 읽는다
  (패치 적용된 게임 폴더를 읽으면 일본어 원문이 사라져 치환이 조용히 0건이 된다).
- 편집 대상: `translated_ko/`(시나리오 평문), `patch_data/`(선택지·DB·맵 텍스트)
- `patch/scenario/`·`patch/www/data/`는 빌드 산출물 — **직접 수정 금지**, 반드시 `build_patch.py`로 재생성
- `patch/www/js/`·`patch/www/fonts/`는 손으로 관리하는 정적 파일이다.
  `plugins/*.js`는 게임 원본 플러그인의 **수정 사본**(TS_Localize·RTK1_Option_EnJa·TS_ADVsystem)이므로
  게임이 업데이트되면 원본과 diff해서 우리 변경점만 다시 얹어야 한다.
- 텍스트가 있는 곳은 `translated_ko/`·`patch_data/`만이 아니다. **플러그인 안에도 표시 문자열이 있다**:
  `TS_Localize.js`의 `LanguageList`(UI 용어)와 `ChangeList`(세이브 화면 지역명 + 스테이터스 화면 상대 이름),
  `RTK1_Option_EnJa.js`의 `M._terms_E`(메뉴·전투 용어)와 옵션 항목 라벨. 여기 없는 문자열은 원문이 그대로 나온다.
- **`translated_ko/`가 번역의 정본이다.** `inject_units.py`는 최초 조립용이므로 다시 실행하지 않는다
  (검수로 교정한 화자 표기 — 예: rp12의 `[편의점 점장]` — 이 name_map 기준으로 되돌아간다).
- **개행은 LF 고정.** 원본 `.sl`이 LF이므로 CRLF가 섞이면 엔진이 읽는 텍스트에 `\r`가 들어간다.
  `.gitattributes`가 `translated_ko/**`·`source_*/**`를 `eol=lf`로, `patch/**`를 `-text`로 고정하고,
  `build_patch.py`도 인코딩 직전에 CRLF를 제거한다.

## 1. 번역 기준 문서가 최우선

- [GLOSSARY.md](GLOSSARY.md)(용어집)와 [STYLE_GUIDE.md](STYLE_GUIDE.md)(문체 가이드)가 모든 번역의 구속력 있는 기준이다.
- **새 용어·표기를 결정하면 먼저 GLOSSARY.md에 추가한 뒤 사용한다.** 문서에 없는 표기를 임의로 쓰지 않는다 (일관성 붕괴 = 저품질).
- 표기 원칙:
  - 일본 인명·지명은 커뮤니티 통용 한글 표기 (사쿠라, 츠카사, 스도…) — `tools/name_map.json`이 화자 태그의 최종 기준
  - 금액은 아라비아 숫자+콤마+엔: "15,000엔"
  - UI는 명사형 종결("저장", "게임 종료"), 지문은 "-다"체, 대사는 캐릭터별 말투(STYLE_GUIDE 표 참조)
  - 성인 표현은 무검열 — 원문(중국어판 기준) 수위 그대로. 완곡화는 오역으로 취급

## 2. 맥락 없는 번역 금지

- 문자열 하나만 보고 번역하지 않는다. 반드시 같은 파일의 **전후 라인 + source_en + source_ch 3판 대조**로 맥락을 확인한다.
- 영어판은 인칭 붕괴·화자 태그 오배치·음역 잔재가 상습적이다 (STYLE_GUIDE §3). 의미가 갈리면 중국어판을 따른다.
- 사쿠라 시점 독백·내레이션은 전부 1인칭으로 복원한다.
- UI·선택지 문자열은 표시 폭 제약을 고려해 짧게 쓴다 (긴 번역은 레이아웃을 깨뜨린다).

## 3. 기술 품질: 불가침 요소와 폰트

- **절대 훼손 금지**: `@…` 엔진 명령줄, `;;` 주석, `*라벨`, `[화자/보이스ID]`의 보이스 ID,
  `\C[n]…\C[0]`, `\CL`, `\V[n]`, `\{…\}`, `\G`, `%1`, 전각 공백(`　`), `♪`/`……`/`――`/`～`
- 번역 출력은 항상 **한 줄** (엔진이 자동 줄바꿈). 단, 자동 줄바꿈 폭에는 한국어용 보정이 필요하다:
  - `TS_ADVsystem.viewMesAdjust()`는 **글자 수**로 줄을 끊고(반각·전각 모두 1자), 원본은 영어 슬롯일 때
    폭이 좁은 라틴 문자를 가정해 한 줄 허용치를 27→**54자로 2배** 늘린다.
    한글은 전각 폭이라 이 값으로는 메시지 창(1024px − 패딩 36px = **988px**)을 넘어 오른쪽이 잘린다.
  - 그래서 `patch/www/js/plugins/TS_ADVsystem.js`가 En 슬롯의 레이아웃을 두 곳에서 재정의한다.
    이 override를 지우면 대사 1,100여 행이 잘린다.
    - 한 줄 허용치 **43자** (전체 대사 실측 최대 961px, 모든 메시지 4행 이내)
      - **잘림선 988px** = 실제로 글자가 안 보이기 시작하는 창 경계.
        **안전선 964px** = `check_line_width.py`가 경고하는 선(잘림선 − 한글 1자). 둘을 혼동하지 말 것.
      - 44자는 최대 977px로 아직 잘리지 않고 완충 구간에만 들어간다. 실제 잘림은 45자(최대 1016px)부터.
      - 43자는 잘림선까지 27px(≈1자)뿐이므로, 텍스트를 고치면 폭 검사를 반드시 다시 돌린다.
    - 왼쪽 들여쓰기 **전각 1/2/3칸** (원본은 5/6/7칸 — 한글에는 여백이 과하다).
      들여쓰기 칸도 허용치에 포함되므로 두 값은 함께 조정해야 한다.
  - 맵의 Show Text도 같은 함수를 거친다 (`command401` 오버라이드). 반면 **선택지·DB 이름/설명/프로필은
    자동 줄바꿈이 없다** — 한 줄이 988px을 넘지 않게 문장을 짧게 쓰거나 직접 끊어야 한다.
  - 검사: `uv run tools/check_line_width.py` (실제 폰트 메트릭으로 전 경로 측정. `python`을 붙이지 않는다 —
    인라인 의존성 선언을 uv 스크립트 러너가 처리해야 fontTools가 설치된다).
  - 시나리오 본문에 리터럴 `\n`을 쓰면 위 함수가 실제 줄바꿈으로 바꿔준다. 단 **DB 노트(`<en:>`)에서는
    무효 이스케이프로 먹히므로** 줄을 나누려면 노트 값 안에 실제 개행을 넣는다 (원본 관례와 동일).
- 폰트: `patch/www/fonts/Pretendard-Regular.otf` 번들 (SIL OFL 1.1, 라이선스 파일 동봉 필수).
  - 엔진이 Chromium 41 세대라 **가변 폰트 사용 금지** — 정적 OTF/TTF만.
  - 폰트 교체 시 재배포 가능한 오픈 라이선스(OFL)인지 반드시 확인.
- 일본어 장음 기호 `ー`는 한국어 텍스트에 남기지 않는다 (`~` 등으로 치환).

## 4. 플레이스홀더와 조사 처리

- `%1`, `\V[n]` 등 변수 삽입 문장은 한국어 어순에 맞게 재배치한다.
- 조사는 **병기 표기**를 쓴다: "%1을(를)", "%1이(가)", "%1은(는)" — RTK 용어(`patch/www/js/plugins/RTK1_Option_EnJa.js`)에 적용된 관례.
- 받침 자동 판별 로직 주입은 하지 않는다 (엔진 코드 수정 최소화 원칙).

## 5. LQA (검수)

번역 완료는 절반이다. **텍스트 수정 후 커밋 전에는 반드시** 스크립트 검증을 통과해야 한다 (인게임 로딩은 느리므로 스크립트 검증이 1차 게이트):

1. `uv run python tools/review_scan.py` — 용어 드리프트·잔존 외국어(가나/한자)·기호/코드 짝 기계 검사
2. `uv run python tools/verify_translation.py` — 구조 무결성 (에러 0 유지 필수)
3. `uv run tools/check_line_width.py` — 표시 폭 (창 밖으로 넘치는 행 0 유지 필수)
4. `uv run python tools/check_untranslated.py` — `translated_ko/` **밖의** 미번역 잔존
   (En 슬롯이 표시하는 한글 없는 문자열, 맵 노트·상대 이름의 ChangeList 미등록, `<en:>` 없는 DB 행,
   한국어 절반이 없는 System.json 용어). 게임 폴더가 있으면 패치 대상 외 맵·DB까지 함께 본다. 0 유지 필수
5. 인게임 확인 — 잘림·겹침·영어 잔존·깨진 문자, 평소 안 보이는 문자열(힌트 단말·에러류)까지
- 대량 검수는 3판 대조 + BEFORE/AFTER 수정안 + `tools/apply_review.py`(원문 일치·보이스 ID 보존 검증) 절차를 따른다.
- 검사 규칙 철학: **'항상 틀린' 형태만 오류로 등록**한다 (오탐이 작업을 막지 않도록).
  오탐이 발견되면 규칙을 삭제하거나 allowlist(`review_scan.py`의 `LATIN_ALLOW` 등)에 편입한다.
  새 '규칙형' 오류를 발견하면 `FORBIDDEN`에 편입해 재발을 막는다.

## 5-0. 검토 이력과 커버리지 (사실 관계)

"전수 검토"라고 뭉뚱그리지 말고 라운드별로 무엇을 했는지 구분해서 말할 것.

| 라운드 | 방법 | 결과 |
|---|---|---|
| 1차 | 690파일 전문 3판 대조 정독 | 64건 반영 (구조 결함 위주) |
| 2차 | 690파일 전문 3판 대조 재정독 | 140건 반영 + 파이프라인 결함 2건 |
| 3차 | 기계 검사 + 신규 검사축 전수 + `rp01_*` 정독 | 66건 반영 (지문 시점 63, 인용부호 3) |
| 4차 | 기계 검사 + 신규 검사축 전수 (중복 번역·보이스 정합·문체 일관·패치 무결성) | 774건 반영 (구두점 770, 문체 1, 표기 3) |
| 5차 | `translated_ko/` 밖 전 표시 경로 전수 (언어분기 선택지·플러그인 표시표·미패치 데이터 파일) | 55건 반영 (선택지 14, ChangeList 24, System 용어 5, DB 2, 옵션 라벨 1 + 검사축 1종) |

- 5차에서 확인된 사실: 시나리오·`" ||| "` 선택지·DB `<en:>`는 잔존 0. 미번역은 전부 **검사축이 없던 경로**에 있었다.
  Skills·States·Enemies·Troops와 `System.json`의 전투 용어는 **전투(명령 301)가 한 건도 없어** 사문 데이터다.
- 대사 전문을 사람이 읽는 작업은 **1·2차에서 완료**했다. 3차 이후는 새 검사축을 전수로 돌리는 방식이다.
- `rp01_*` 12개 파일은 3차에서 중국어판 대조 정독을 마쳤다 (과거 "미검토" 기록은 해소됨).
- 검사축은 누적된다: 새 축을 만들면 스크립트로 남겨 다음 라운드에서 재사용한다.

## 5-1. 미번역 유지 정책 (의도적 영어/원문 유지)

다음은 번역하지 않고 원문을 유지한다 — `review_scan.py`의 `LATIN_ALLOW`가 기준선:

- 실존 고유명·제품명: LINE(메신저), KFC, VIP, BGM, TV, VR, CG, WASD, No.1, DVD 등
- 연출상 영어 대사: 「Beautiful!!」 (EN·CH 공통의 의도적 영어 감탄)
- 보이스 ID(skr…)·이미지 파일명·라벨(*…)·엔진 명령 파라미터 전부
- 맵 데이터에서 유지하는 것: CG 번호 선택지(`CG03`…), 플러그인 태그(`<limit:5>`),
  스위치로만 열리는 디버그 이벤트의 선택지(`選択肢1`…) — `check_untranslated.py`의 `ALLOW_NO_HANGUL`이 기준선
- 사문 데이터는 번역하지 않는다: Skills·States·Enemies·Troops 전체와 미사용 DB 행
  (VR 게임용 Actors#2~5·Classes#2~6·Items#1~6, 에디터 구분선 `武器一覧`·`衣装一覧ーーー` 등).
  판정 근거는 획득(126~128)·장비(319)·파티 합류(129)·전투(301) 참조가 0건이라는 것이며,
  `check_untranslated.py`가 매번 다시 판정하므로 게임이 업데이트돼 쓰이기 시작하면 자동으로 잡힌다.
- **그림 안에 박힌 텍스트는 번역 불가**: `img/pictures/*_en.rpgmvp`는 영어가 새겨진 이미지이며
  한국어 슬롯에서 그대로 표시된다 (암호화 PNG 재작성이 필요해 범위 밖).

## 5-2. 원문 결함·의도적 결정 목록 (회귀 금지)

의도적 결정이 나중에 "고치는" 회귀로 뒤집히지 않도록 기록한다. 새 결정이 생기면 여기 추가한다.

**원문 결함이지만 그대로 유지 (동작 동일성):**
- `mob0041.txt` KFC 역주(`ps：…`) — EN·CH 모두에 실존하는 게임 텍스트. 삭제 금지.
- EN 원문 자체가 깨진 따옴표(예: rp19 `「…)`)는 발견 시 문맥에 맞는 짝으로 복구한다 (표시 품질 우선).

**EN과 다르게 간 의도적 개선 (CH/개발 의도 기준) — 전체 목록은 `tools/verify_translation.py`의 `WAIVERS`:**
- EN에서 실수로 노출된 개발 메모("Lascivious degree above 30" 등)는 CH처럼 `;;` 주석 처리한다.
- EN이 누락한 강조 코드(`\{…\}` 등)는 CH 기준으로 복원한다 (가챠코 기동음 등).
- 일본어 장음 `ー`는 `~`로 치환한다 (EN·CH가 남겨도 한국어 텍스트에는 유지하지 않음).
- EN이 훼손한 엔진 명령은 CH 기준으로 복원한다 (@wait 오염, @명령 선행 공백, @se가 텍스트로 치환된 것 등).
- `@select` 선택지 문구는 한국어로 번역한다 (값 내 단어 구분은 전각 공백 유지 — 반각 공백은 파라미터 구분자라 금지).
- **보이스 ID가 EN에서 중복/오류인 경우 CH 기준으로 교정한다** — 단, 대상 음성 파일(`www/audio/voice/`)이
  실존하는지 반드시 확인 후 적용 (rp03#290 skr0507, rp10#140 skr0996, rp10#162 skr1001 사례).
- 새 항목이 생기면 `WAIVERS`에 (파일, 줄, 사유)를 등록해야 구조 검증이 통과한다.

**5차에서 추가한 의도적 결정:**
- **언어 무관 문자열을 한국어로 치환하면 ja/ch 슬롯도 함께 바뀐다.** 번역본이 애초에 없는 경우에만
  허용한다 (`event_ui_ko.json`의 회차 시작 선택지 3개와 `ＯＫ`/`ＮＯ`, 맵의 `map_text_ko.json`과 같은 판단).
  언어 분기가 있는 선택지는 반드시 En 분기만 건드린다 (`map_lang.apply_to_visible`).
- `TS_Localize.ChangeList`에 새로 넣는 항목의 **중국어 열은 일본어 원문을 그대로 둔다** —
  게임에 대응 번역이 없고, 키가 없을 때와 동작이 같아 중국어 슬롯에 회귀가 없다.
- `System.json`은 배열을 갈아치우지 않고 `"持ち物||소지품"` 형태로 **두 언어를 한 문자열에 담는다**
  (RTK `updateTypeData`가 분리). 배열을 통째로 바꾸면 ja/ch 슬롯까지 한국어가 된다.
- 원본에 `<en:>` 태그가 아예 없는 DB 행(Armors#4·#11)은 `build_patch.py`가 태그를 **새로 붙인다**.
- `ExpStatus`의 상대 이름은 스테이터스 창에서 폭 160px에 그려진다(캔버스가 자동 압축).
  긴 표기(`초자연연구부 부장` 등)는 압축을 감수하고 용어집 표기를 따른다 — 새 약어를 만들지 않는다.

## 6. 유지보수 구조

- 게임 업데이트 시: `scenario/En`을 재디코딩(`sl_codec.py`)하여 `source_en/`과 diff → 추가·변경분만 재번역 → 재조립.
- 수정 워크플로우: `translated_ko/` 또는 `patch_data/` 편집 → `build_patch.py` → `apply_patch.py`.
- 원상 복구는 **Steam 게임 파일 무결성 검사**로 대체한다 — 패치 도구에 백업 기능을 두지 않는다.

## 7. 작업 규칙 (사용자 지정)

- **Git**: 작업 단위마다 한국어 메시지로 항상 커밋한다. **푸시 금지** — 사용자가 검토 후 직접 푸시한다.
  커밋 author는 사용자 계정 그대로, **Co-Authored-By: Claude 트레일러 금지**.
- **Python**: 스크립트 실행은 항상 uv를 사용한다 (`uv run python …`).
- **문서 서식** (README 등 마크다운):
  - 섹션 제목마다 이모지를 넣는다.
  - 문장이 끝나면 줄바꿈하되, GitHub 렌더링에 반영되도록 **하드 브레이크(줄 끝 공백 2개)**를 쓴다.
  - 파일/폴더 목록은 **폴더 우선, 각각 abc 순**으로 정렬한다.
- **리포지토리 위생**: 일회성 중간 산출물(작업 데이터, 검수 리포트 등)은 커밋하지 않는다 — gitignore 처리 (`review/` 사례).
- **원본 불변**: `source_en/`·`source_ch/`는 대조용 불변 자료다 — 수정 금지. 편집은 항상 `translated_ko/`·`patch_data/`에만.
- **대량 수정은 스크립트로**: 손으로 하지 말고 일회성 스크립트로 수행한다 (재현 가능·검증 용이).
  일회성 스크립트는 반영 후 삭제하고 변경 이력은 git 커밋으로 추적한다. 상시 도구만 `tools/`에 보존한다.

## 파이프라인 명령 요약

```
uv run python tools/sl_codec.py decode|encode <in> <out>   # .sl ↔ 평문
uv run python tools/build_patch.py                         # patch/ 재조립 (인코딩+JSON)
uv run python tools/apply_patch.py [game_dir]        # 게임 적용 (복구는 Steam 무결성 검사)
uv run python tools/review_scan.py                         # 기계 검수
uv run python tools/verify_translation.py                  # 구조 검증
uv run tools/check_line_width.py                           # 표시 폭 검증 (python 없이 실행)
uv run python tools/check_untranslated.py                  # translated_ko 밖 미번역 검사
uv run python tools/apply_review.py                        # 검수 수정안 일괄 적용
```

요약: **용어집·스타일 가이드 → 3판 대조 맥락 번역 → 코드/폰트 검증 → 기계+인게임 LQA → 커밋**.
이 파이프라인을 지킨 패치와 텍스트만 바꾼 패치는 품질 차이가 확연하다.
